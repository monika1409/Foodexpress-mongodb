# MongoDB connection string
# For local MongoDB: mongodb://localhost:27017/foodexpress
# For MongoDB Atlas: mongodb+srv://<username>:<password>@cluster0.xxxx.mongodb.net/foodexpress?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/foodexpress

# Admin panel password
ADMIN_PASSWORD=foodexpress51

# Port the server runs on
PORT=3000


# Email settings for OTP verification codes (sign in / sign up)
# Any SMTP provider works — Gmail, Outlook, SendGrid, Mailgun, etc.
# For Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, and SMTP_PASS must be an
# "App Password" (Google Account → Security → App Passwords), not your normal password.
PORT=3000
ADMIN_PASSWORD=foodexpress51
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=monikamathiyalagan2004@gmail.com
SMTP_PASS=zrkuphbtqtxsczke
SMTP_FROM="Food Express <monikamathiyalagan2004@gmail.com>"